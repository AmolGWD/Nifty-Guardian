import './theme.css'
import { DashboardPage } from '../pages/Dashboard'

/**
 * Root shell. Phase 1's backend-connectivity placeholder page is
 * retired here - the Paper Trading Engine (Phase 20) is now real and
 * this dashboard (Phase 21) is its operational console, so there is
 * no longer a "coming soon" page to keep around.
 */
export function App() {
  return <DashboardPage />
}
