"""
REST API for the Signal Engine - a new, additive surface, parallel to
`app.api.dashboard` (Phase 22, frozen, untouched). Exposes the Guardian
Score, current market bias, dummy trade history, and daily performance
reporting produced by `app.signals`/`app.notifications`.
"""
