APP_NAME=NIFTY Guardian v2
ENVIRONMENT=development
LOG_LEVEL=INFO

HOST=0.0.0.0
PORT=8000

CORS_ORIGINS=http://localhost:5173

# Required. Generate your own with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
SECRET_KEY=

# Zerodha Kite Connect. Register your redirect URL as
# http://localhost:8000/auth/kite/callback in the Zerodha developer console.
KITE_API_KEY=
KITE_API_SECRET=
